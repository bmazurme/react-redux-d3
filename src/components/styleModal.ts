import { CSSProperties } from 'react';

const buttonCancelStyle: CSSProperties = { width: 'calc(50% - 8px)', margin: '8px 8px 8px 0' };
const buttonOkStyle: CSSProperties = { width: 'calc(50% - 8px)', margin: '8px 0px 8px 8px' };
const selectStyle: CSSProperties = { width: '100%', margin: '8px 0' };

export { buttonCancelStyle, buttonOkStyle, selectStyle };
